@model IEnumerable<RT334015_MIS4200.Models.Appointment>

@{
    ViewBag.Title = "Appointment";
}

    <h2>Appointment</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered table-hover table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Doctor.doctorFirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.apptDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Doctor.doctorFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.apptDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.appointmentId }) <span class="glyphicon">&#x270f;</span>|
            @Html.ActionLink("Details", "Details", new { id=item.appointmentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.appointmentId }) 
        </td>
    </tr>
}

</table>
